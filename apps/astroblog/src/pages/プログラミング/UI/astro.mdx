import Layout from '../../../layouts/Layout.astro';
import { Counter } from '../../../components/Counter.tsx';

<Layout>
# Astroでサイト作ってみた

|       | Katex | Hydrate | Global Layout |
|-------|-------|---------|----------------|
| Hono  | ×     | ×       | ○              |
| Vike  | ○     | ×       | ○              |
| Astro | ○     | ○       | ×              |

astroが一番安定していて使いやすかったです。

honoはreact renderer試したけどバグり散らかしてよくわからなかった...

↓ mdxの中でreact componentを使う例
<Counter client:load/>

↓ katexも書ける
```math
\frac{d}{dz} A^{\omega}(z) = -j\kappa^{*}[A^{\omega}(z)]^{*} A^{2\omega}(z) \exp(-j(2\Delta)z), \tag{2.59a}
```

```math
\frac{d}{dz} A^{2\omega}(z) = -j\kappa[A^{\omega}(z)]^{2} \exp(+j(2\Delta)z), \tag{2.59b}
```

```math
2\Delta = \beta^{2\omega} - 2\beta^{\omega} \tag{2.60}
```

```math
\kappa = \frac{2\omega \epsilon_0}{4} \iint [E^{2\omega}(x,y)]^{*} d(x,y)[E^{\omega}(x,y)]^{2} dxdy. \tag{2.61}
```

上の比較にもあるが、layoutは以下のようにしてfrontmatterごとに明示的に指定する必要がある

```typescript
import Layout from '../../../layouts/Layout.astro';
<Layout>
page content
</Layout>
```

mdxじゃ補完がきかないからめんどくさい...

viteのpluginで頑張って、コンパイル前にlayoutを挿入するか、tanstack routerみたいにファイル作成時に自動でlayoutを挿入するようにしたい

</Layout>
